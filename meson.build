project('pw-osc-modules', 'cpp', default_options: ['cpp_std=c++23'])

pipewire_dep = dependency('libpipewire-0.3')
liblo_dep = dependency('liblo')
boost_dep = dependency('boost', modules : ['system'])

subdir('src')